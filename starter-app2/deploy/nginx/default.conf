server {
    listen 80;

    location / {
        resolver 127.0.0.11 valid=5s;
        set $backend "app-blue:5000";

        proxy_pass http://$backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
